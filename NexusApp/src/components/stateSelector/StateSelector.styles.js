import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginTop: 20,
    width: '90%',
    backgroundColor: '#111',
    borderRadius: 12,
    padding: 10,
  },
  label: {
    color: '#0ff',
    fontSize: 14,
    marginBottom: 4,
    fontWeight: 'bold',
  },
  picker: {
    backgroundColor: '#222',
    color: '#fff',
    marginBottom: 10,
  },
})
